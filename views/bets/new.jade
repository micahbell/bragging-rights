extends ../layout

block content
  h1 Add New Bet
  
  form(action='/bets/create' method='post')
    include ./includes/bet-form
    input(type='submit' value='Add Bet')
  
  a(href='/bets/index') Back to All Bets
  
